# Arduino build artifacts
build/
*.elf
*.bin
*.hex
*.map

# Arduino IDE cache
.arduino15/
sketches/

# Windows
Thumbs.db
Desktop.ini
ehthumbs.db

# macOS
.DS_Store
.AppleDouble
.LSOverride

# VS Code
.vscode/
*.code-workspace

# Logs y datos de vuelo (no subir datos de misión al repo público)
logs/
*.csv
TELEM_*.csv
FLIGHT_*.bin

# aero_table.h generada — solo subir el placeholder
# Descomenta si quieres excluir la tabla real:
# aero_table.h

# Calibración con datos reales (contiene parámetros de hardware específico)
# cal_data.bin

# Notas privadas
*.private.*
NOTES_*.md
